/*
just because it looks like a file path doesn't mean it physically is

URL Routing = using code to navigate the url accordingly and makes sure you end up with error 404 if the inputed url doesn't exist

tld = top level domain e.g .com .co.uk .org
main domain, what's left of the tld
sub domain, to the left of the main domain
domains seperated by '.'

in a website '?' begins a query
'&' connects together multiple queries
deep links allow you to get the same page every time










*/